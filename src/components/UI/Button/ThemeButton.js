import styled from "styled-components";
import { space, color, border, typography } from "styled-system";

const ThemeButton = styled("button")(space, color, border, typography);

export default ThemeButton;
