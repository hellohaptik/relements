import { CodeBlock, ConfigBlock } from "@src/helpers/DocBlocks";
import dayjs from "dayjs";
import Date from "./Date";
import Icon from "@src/components/UI/Icon";

# DateInput

> A clickable UI element with active and hover states. Indicates to the user that it's clickable.

<CodeBlock defaultValue={dayjs()}>
  {(value, onChange) => (
    <Date label="Select Date" value={value} onChange={onChange} numMonths={2} withTime/>
  )}
</CodeBlock>

## Configuration

<ConfigBlock of={Date} />

## Examples

<CodeBlock title="Single" defaultValue={dayjs()}>
  {(value, onChange) => (
    <Date label="Select Date" value={value} onChange={onChange} numMonths={2} />
  )}
</CodeBlock>

<CodeBlock
  title="Range"
  defaultValue={{ startDate: dayjs(), endDate: dayjs().add(7, "d") }}
>
  {(value, onChange) => (
    <Date
      label="Select Date"
      value={value}
      onChange={onChange}
      numMonths={2}
      withRange
    />
  )}
</CodeBlock>

<CodeBlock
  title="Comparison"
  defaultValue={{ startDate: dayjs(), endDate: dayjs().add(7, "d") }}
>
  {(value, onChange) => (
    <Date
      label="Select Date"
      value={value}
      onChange={onChange}
      numMonths={2}
      withRange
      withComparison
    />
  )}
</CodeBlock>

<CodeBlock
  title="Postfix node as Relements Icon Component"
  defaultValue={dayjs()}
>
  {(value, onChange) => (
    <Date
      label="Select Date"
      value={value}
      onChange={onChange}
      postfixNode={<Icon src="plusCalendar" size="small" />}
    />
  )}
</CodeBlock>

<CodeBlock
  title="Postfix node as html element"
  defaultValue={dayjs()}
>
  {(value, onChange) => (
    <Date
      label="Select Date"
      value={value}
      onChange={onChange}
      postfixNode={<div>$</div>}
    />
  )}
</CodeBlock>
